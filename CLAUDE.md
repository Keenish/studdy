# studdy — 작업 규칙

iOS 학습 정리 저장소. **독립 저장소**이고 다른 레포와 의존 관계가 없다.

성격이 튜토리얼이 아니라 **검증 기록**이라서, 아래 규칙 대부분이 "무엇을 근거로 쓸 수 있는가"에 관한 것이다. 사람이 읽는 진입점은 [README.md](README.md).

---

## 1. 공개 저장소다 — 비공개 자산을 들이지 않는다

이 저장소는 GitHub에 **public**으로 공개돼 있다. 가장 지키기 어렵고 가장 중요한 규칙이다.

**넣지 않는다**

- 회사·고객 코드의 원문 인용, 타입명·파일명·모듈 경로
- 비공개 디자인 시스템의 토큰 값·컴포넌트 스펙·브랜드 색
- 사내 문서(정책서·분석 문서)의 인용과 경로
- 워크스페이스 상대 경로(`../<다른레포>/…`) — 이 저장소 밖을 가리키는 링크
- 벤더·SDK 이름으로 드러나는 거래 관계, 미출시 제품명, 번들 ID

**대신 이렇게 쓴다**

- 실습 코드는 **직접 쓴다.** 소재가 실무에서 왔더라도 이름·값은 새로 정한다
- 팔레트·치수 같은 값은 **예제로 직접 정하고** "실습용 예제 값"이라고 밝힌다
- 비공개 코드베이스를 측정했다면 **수치를 싣지 않고 방법(명령)과 판정의 모양만** 남긴다. 실제 사례가 [Phase 4 §3](Refactoring/phase4-large-scale-refactoring.md#3-적용--절차를-실제-코드베이스에-돌렸다)
- 근거가 빠진 자리는 비워 두지 말고 **"확인하지 못한 것"으로 명시한다**

> 2026-07-31에 이 규칙을 어긴 상태를 전수 정리하고 히스토리를 단일 커밋으로 재작성했다. 되돌리는 비용이 크므로 **커밋 전에 확인한다.**

확인 명령:

```bash
git grep -i -E "<회사명>|<제품명>|<사내레포명>" -- .
```

---

## 2. 증거 규칙

문장마다 검증 강도가 다르고, 그 차이를 표기로 드러낸다.

| 표기 | 뜻 |
|---|---|
| (라벨 없음) | 실행·컴파일 출력으로 확인함 |
| `[중]` | 널리 알려졌지만 여기서 측정하지 않음 |
| `[저]` | 계획·가설·경험 규칙. 아직 해보지 않음 |
| **검증되지 않음** | 시도했으나 환경·권한 때문에 실패. 이유를 같이 적는다 |

- **라벨 없는 문장은 근거가 있어야 한다.** 없으면 라벨을 붙이거나 문장을 고친다
- 인용하는 출력은 **실제 출력을 그대로** 붙인다. 손으로 다듬으면 그 순간 검증이 아니다
- **틀린 결과도 지우지 않는다.** 왜 틀렸는지를 함께 남긴다 ([사례 6](AI/phase-parallel-ai-verification.md#6--컴파일러가-잡아주지-못한-유일한-사례))
- 발견을 **결함으로 단정하지 않는다.** SoT가 어디인지 먼저 확인한다

---

## 3. 검증 명령

```bash
bash verify_all.sh            # 전부 (15개)
bash verify_all.sh --quick    # 빌드·테스트·링크만
python3 AI/code/check_links.py  # 문서 링크·앵커 전수 검사
```

**주의할 것 셋** — 전부 이 저장소에서 실제로 데인 것들이다.

- **`-typecheck`으로 검증하지 않는다.** strict concurrency의 region isolation 진단은 SIL 패스에서 나오고 `-typecheck`은 **0건** 잡는다. `-c`나 `swift build`를 쓴다 (사례 21)
- **0이 나오면 장치부터 의심한다.** "위반 없음"과 "검사가 안 돌았음"은 출력이 같다. 가드를 만들었으면 **일부러 깨뜨려** 잡히는지 본다 (사례 17·19·22)
- **세는 명령의 종료 코드를 본다.** `wc -l`·`grep -c`는 실패를 0으로 바꾼다. 특히 그 수치가 삭제·덮어쓰기의 근거일 때 (사례 12·24)

전체 목록은 [§4-B 체크리스트](AI/phase-parallel-ai-verification.md#7-체크리스트).

---

## 4. 문서 규칙

**구조** — 각 정리 문서는 같은 골격을 갖는다.

```
# Phase N — 제목
  범위 · 코드 · 근거 (머리말)
## 1..N  본문
## 스스로 물어볼 것     ← 이해 점검용 질문
## 검증 기록
  ### 환경
  ### 실행한 것          ← 명령과 결과
  ### 확인하지 못한 것   ← 라벨 붙은 표
## 참고 자료
  (다음 문서로 잇는 한 줄)
```

**수치는 한 곳에만 둔다.** 인덱스(`README`)에는 "무엇을 찾았나"만 적고 값은 본문을 가리킨다. 같은 숫자가 두 곳에 있으면 반드시 한쪽이 낡는다 — 이 저장소에서 실제로 여러 번 일어났다.

**건수를 제목·요약에 박지 않는다.** "사례 10건" 같은 표기는 사례가 늘면 즉시 낡는다. 세고 싶으면 원본을 연다.

**사례를 추가할 때는 매핑도 같이 본다.** [§4 §1 표](AI/phase-parallel-ai-verification.md#1-실제로-틀렸던-것들)에 사례를 더하면 [§2 탐지 수단 표](AI/phase-parallel-ai-verification.md#2-무엇이-무엇을-잡는가)의 어느 행에 들어가는지 확인한다. 실제로 두 건이 누락된 채 방치된 적이 있다.

**폴더 배치** — study_list의 묶음(§1~§6)이 폴더다. 코드는 `<묶음>/code/`, 테스트는 `<묶음>/code/<phase>Tests/`.

---

## 5. 코드 규칙

- 언어 모드는 **항상 `-swift-version 6`**. 낮은 모드의 통과는 통과가 아니다
- 외부 라이브러리가 필요하면 루트 `Package.swift`에 타겟을 추가하고, **`path:`를 기존 `<묶음>/code/` 폴더로 지정**한다. `Sources/`를 새로 만들지 않는다
- 의존성 없는 실습은 `swift <파일>`로 단독 실행한다
- 스냅샷 참조 이미지는 `__Snapshots__/`에 커밋한다. 팔레트·치수를 바꾸면 **재기록이 필요하다**

---

## 6. 커밋

- 커밋 메시지는 한국어. **무엇을 했나보다 무엇을 알게 됐나**를 적는다. 기존 메시지들이 그 형식이다
- 수치를 메시지에 적을 때는 그 수치를 낸 명령도 함께
- 커밋 전 §1의 grep과 `bash verify_all.sh --quick`
